#include "Player.h"

void NAME_COLOR_f(player_t *player, uint objectId, uint subId, uint color)
{
/*	Player_Writer2(player, OpCodes_GetOpCode(player, NAME_COLOR));
	Player_Writer4(player, objectId);
	Player_Writer4(player, subId);
	Player_Writer4(player, color);
	Player_UpdateHeaderS(player);*/
}
