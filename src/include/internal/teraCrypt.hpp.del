#ifndef _TERACRYPT_HPP_
#define _TERACRYPT_HPP_

class teraCrypt
{
	public:
		teraCrypt(unsigned char *CKey1, unsigned char *CKey2, unsigned char *SKey1, unsigned char *SKey2);
		~teraCrypt();
		void	encode(unsigned char *data, int size);
		void	decode(unsigned char *data, int size);
		bool	reversed;
  
	protected:
		void *TCrypt;
};

#endif // _TERACRYPT_HPP_
